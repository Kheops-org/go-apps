schemaVersion: 2.2.2
metadata:
  name: golang
components:
  - name: tools
    container:
      image: quay.io/devfile/golang:latest
      memoryLimit: 3Gi

commands:
  - id: run-application
    exec:
      label: "Run server.go"
      component: tools
      workingDir: '${PROJECT_SOURCE}'
      commandLine: "go run server.go"
      group:
        kind: run
        isDefault: true
